# Utils (`utils/`)

Ancillary directory utilized for system-level helper structures, formatting tools, serialization mechanisms, and context formatting needed globally throughout the project. 

Modules here act as purely stateless functional processors transforming data strings into optimized structures (ex: mapping parsed presentation lists into truncated LLM instruction context strings) maintaining standard boundaries for upstream agents.
